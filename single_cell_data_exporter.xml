<tool id="export_to_genap2" name"Single cell data exporter" version="0.1">
    <command detect_errors="aggressive"><![CDATA[
python '$__tool_directory__/single_cell_data_exporter.py' -d $dataset_name
#for $d in $datasets
    '${d}' '${d.hid}: ${d.element_identifier}' '${d.ext}'
#end for
> '$log'
    ]]></command>
    <inputs>
        <param name="datasets" type="data" format="data" multiple="true" label="Datasets to export" />
		<param name="dataset_name" label="How do you want to label your dataset?" type="text" format="text" help="Give a unique and representative name to your dataset which will be seen in the shiny app.">
    </inputs>
    <outputs>
        <data name="log" format="txt" />
    </outputs>
    <tests>
    </tests>
    <help><![CDATA[
Simple tool to copy datasets to a Rshiny readable directory.
This tool is meant to be used as part of the Single Cell pipeline and it is of no use on its own.
    ]]></help>
    <citations/>
</tool>
